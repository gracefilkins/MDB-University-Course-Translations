안녕하세요. 이 비디오에서는 색인에 대해 학습할 것이다. 색인이 무엇인지, 성능을 향상시킬 수 있는 방법, 사용하는 비용에 대해 논의할 것이다. 또한 가장 일반적인 인덱스 유형도 검토할 것이다. 

인덱스는 쉽게 트래버스 및 검색하기 쉬운 정렬된 양식으로 콜렉션 데이터의 작은 부분을 저장하는 특수한 데이터 구조입니다. 인덱스는 문서 를 가리키며 데이터를 더 빠르게 검색, 액세스 및 업데이트할 수 있도록 합니다. 인덱스는 쿼리 성능을 향상시키기 위해 몽골에서 사용됩니다. 쿼리 속도를 높이고 디스크 입출력을 줄이며 필요한 자원을 줄일 수 있습니다. 

인덱스는 또한 동등 일치 및 범위 기반 조작과 같은 쿼리를 지원하고 정렬된 결과를 리턴합니다. 인덱스는 인덱스가 작성될 때 제공되는 인덱스 필드 및 값 정렬 순서에 따라 정렬된 양식으로 데이터를 저장합니다. 인덱스가 없으면, 몽골 는 콜렉션 스캔을 수행하여 쿼리가 실행 중인 쿼리와 일치하는지 확인하여 콜렉션의 모든 문서를 읽어야 합니다. 또한 쿼리에 정렬된 출력이 필요한 경우 메모리의 결과를 정렬해야 합니다. 

인덱스를 사용하는 경우, 몽골은 쿼리를 기반으로 인덱스로 식별된 문서만 페치하고 결과를 더 빨리 리턴합니다. 색인이 조회가 검색하려는 모든 정보를 포함하는 경우, 몽골어는 문서를 읽을 필요가 없습니다. 기본적으로 콜렉션마다 하나의 인덱스만 작성됩니다. 이 기본 색인에는 _ 필드만 포함됩니다. 모든 쿼리는 인덱스를 사용해야 합니다. 이렇게 하면 데이터 쿼리를 다루기 위해 콜렉션에 추가 인덱스를 작성할 수 있습니다. 

인덱스에 쓰기 성능 비용이 발생한다는 점에 유의하십시오. 콜렉션에 새 문서를 삽입하거나 인덱스 필드를 갱신할 때 인덱스 구조의 데이터도 갱신해야 합니다. 또한 콜렉션에 인덱스가 너무 많으면 쓰기 성능이 저하될 수 있습니다. 그래서 우리가 가지고 있는 모든 색인이 사용되고 있는지 확인해야 합니다. 그렇지 않으면 불필요하거나 중복되는 색인을 삭제해야 합니다. 

이제 인덱싱의 기본을 다루었으므로, 몽골 에서 가장 일반적인 인덱스 유형을 살펴보자. 단일 필드 인덱스 및 복합 인덱스. 단일 필드 인덱스는 한 필드에서만 인덱스입니다. 복합 인덱스에는 인덱스에 둘 이상의 필드가 포함됩니다. 두 인덱스 모두에서 시작 필드 또는 인덱스 접두부를 사용하여 쿼리를 지원할 수 있습니다. 두 인덱스 유형 모두 배열 필드에서 작동하는 경우 다중 키 인덱스가 될 수도 있습니다. 각 배열 항목에는 해당 색인 항목이 있습니다. 

고객 콜렉션에 대해 실행된 쿼리를 기반으로 예제를 살펴보자. 이 콜렉션의 문서 예제는 다음과 같습니다. 콜렉션에는 고객에 대한 정보 (예: 사용자 이름, 생일, 활성 상태인 경우), 보유한 계정 및 계정에 대한 기타 세부사항이 포함됩니다. 일반적으로 이러한 고객 조회를 실행하여 활성 상태이고 특정 계정을 가지고 있는 모든 고객을 검색합니다. 

이 경우, 활성 및 계정 필드가 있는 인덱스를 정의하여 이러한 쿼리의 성능을 향상시킬 수 있습니다. 인덱스에는 두 개의 필드가 있고 이들 중 하나는 배열 필드이므로 다중 키 복합 인덱스입니다. 두 조회 모두 이 색인을 사용할 수 있습니다. 

훌륭한 일입니다. 여러분이 이 비디오에서 배우는 것을 살펴보겠습니다. 인덱스는 콜렉션의 일부 필드를 저장하는 정렬된 데이터 구조로, 더 빠른 데이터 검색을 허용하고 계산을 줄일 수 있습니다. 인덱스는 동등 일치 및 범위 기반 쿼리 조작을 지원하고 정렬된 결과를 리턴합니다. 그러나, 이들은 기록 성능 비용을 갖게 된다. 인덱스 필드가 갱신 또는 삽입되면 해당 인덱스 항목도 갱신되어야 합니다. 몽골에서 가장 일반적인 인덱스 유형은 단일 필드 인덱스 및 복합 인덱스입니다. 배열 필드에서 작동하는 인덱스를 다중 키 인덱스라고 합니다. 